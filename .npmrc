# pnpm 11 verifica deps antes de cada script y lanza un install desde el cwd de la
# app (fuera del workspace), rompiendo `next dev`/`build`. Lo desactivamos:
# el install se ejecuta explícitamente desde la raíz.
verify-deps-before-run=false
