# Secreto de Better-Auth — generar con: openssl rand -base64 32
BETTER_AUTH_SECRET=cambia-esto-por-un-secreto-de-32-caracteres-minimo
BETTER_AUTH_URL=http://localhost:3002
# Orígenes con cookies permitidas (dashboard, tenant-site)
TRUSTED_ORIGINS=http://localhost:3000,http://localhost:3001
# Password de los usuarios owner del seed (solo dev)
SEED_OWNER_PASSWORD=cambia-esto
