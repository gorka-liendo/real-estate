// Punto de entrada de los workers BullMQ.
// Cada módulo registrará aquí sus workers cuando exista Redis (Fase E).
console.log("workers: esqueleto listo — los workers se registran cuando haya Redis (Fase E)");
