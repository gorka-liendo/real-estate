# Workers (BullMQ). Sin REDIS_URL quedan idle (ver @rep/queue).
# REDIS_URL=redis://localhost:6379
# QUEUE_PREFIX=rep

# Storage y email los heredan de @rep/storage y @rep/email (drivers local/console
# por defecto). Ver sus .env.example para activar R2/Resend.
